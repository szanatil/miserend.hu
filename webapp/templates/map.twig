{% extends "layout.twig" %}

{% block body %}
    {% include 'header.twig' %}
    <style>
        #mapid {
            position: relative;
            width: 100%;
            height: calc(100vh - 50px);
            margin: 0;
            padding: 0;
        }
        .menusav {
            margin-bottom: 0 !important;
        }
        body {
            margin: 0;
            padding: 0;
        }
    </style>
    {% include '_map_leaflet.twig' with {'enableAutoGeoLocation': true} %}
    {% block footer %}{% include 'footer.twig' %}{% endblock %}
{% endblock %}
    